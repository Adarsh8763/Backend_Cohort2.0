apiVersion: apps/v1
kind: Deployment
metadata:
  name: main-server-deployment
spec:
  selector:
    matchLabels:
      app: main-server
  replicas: 3
  template:
    metadata:
      labels:
        app: main-server
    spec:
      containers:
      - name: main-server
        image: main-server:latest
        imagePullPolicy: Always
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
          requests:
            memory: "64Mi"
            cpu: "250m"
        ports:
        - containerPort: 3000
